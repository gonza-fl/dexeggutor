{
  "name": "devxeggutor",
  "version": "0.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file .env ./src/index.js",
    "dev": "node --watch --env-file .env ./src/index.js"
  },
  "keywords": [],
  "author": "gonza-fl",
  "license": "ISC",
  "description": "Permite a los desarrolladores ejecutar comandos controlados en un servidor (recomendado en test) sin tener que acceder directamente a este",
  "dependencies": {
    "express": "^4.21.2",
    "sqlite3": "^5.1.7"
  }
}
